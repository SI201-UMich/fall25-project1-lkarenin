# Name: Karen Lin
# Student ID: 07134248
# Email: linkaren@umich.edu
# Collaborators: Lilly Joelson
# Karen's Functions: 
# Lilly's Functions: 

import csv

def main():
    inFile = open("penguins.csv")
    penguins_file = csv.DictReader(inFile)

    headers = next(penguins_file)
    print(penguins_file)
    return penguins_file
    



# def species_list(penguins_file):
#     unique_species = []
#     for row in penguins_file:
#         species = row["species"]
#         if species not in unique_species:
#             unique_species.append(species)
#         else:
#             pass
#     return unique_species
#  # [adelie, gentoo, chinstrap]


# FIX: make it so that the output is a dict w keys of unique species and vals are avgs of male bill lengths
# def male_bill(penguins_file, species_list):
    
#     bill_lengths = []
#     for species_name in species_list:
#         for row in penguins_file:
#             if species_name == row["species"] and row["sex"] == "male":
#                 bill_lengths.append(float(row["bill_lengths_mm"]))
#                 avg_male_bill = sum(bill_lengths) / len(bill_lengths)
#                 print(f"(f"Avg male bill length for {species_name}: {avg_male_bill}")")

        

#     return avg_male_bill

# # output: {adelie: 30, gentoo: 40, chinstrap: 50}
            
    


# def fem_bill(penguins_file):
#     for species, bill_length_mm in species_list:
#         if species == "female":
#             avg_fem_bill = sum(bill_length_mm) / len(bill_length_mm)

#     return avg_fem_bill

# def male_flipper(penguins_file):
#     for species, flipper_length_mm in species_list:
#         if species == "male":
#             avg_male_flipper = sum(flipper_length_mm) / len(flipper_length_mm)
            
#     return avg_male_flipper

# def fem_flipper(penguins_file):
#     for species, flipper_length_mm in species_list:
#         if species == "female":
#             avg_fem_flipper = sum(flipper_length_mm) / len(flipper_length_mm)
            
#     return avg_fem_flipper


#def male_species_proportion(avg_male_flipper, avg_male_bill):#


#def fem_species_proportion():


#def max_male_proportion():


#def max_fem_proportion():




if __name__ == "__main__":
    penguins_file = main()
    species_names = species_list(penguins_file)

    with open("penguins.csv") as f:
        penguins_file = csv.DictReader(f)
        for species_name in species_names:
            male_bill(penguins_file, species_name)


# def testcases(penguins.csv):
#     try:
#         with open(penguins.csv, "r") as f:
#             reader = csv.reader(f)
#     except FileNotFoundError:
#         return "File Not Found"
    
#     assert species_list(penguins.csv) == ['Adelie', 'Gentoo', 'Chinstrap']
#     assert male_bill()